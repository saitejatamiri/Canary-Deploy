apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-canary-stable
spec:
  replicas: 3
  selector:
    matchLabels:
      app: flask-canary
  template:
    metadata:
      labels:
        app: flask-canary
    spec:
      containers:
      - name: flask-container
        image: {{.Values.image}}
        ports:
        - containerPort: 5000
